# Generated from @specs/features/authentication/spec.md
# Backend Environment Variables

# Database connection (Neon PostgreSQL)
DATABASE_URL=database_url

# Better Auth shared secret for JWT verification (fallback)
# IMPORTANT: Generate with: openssl rand -base64 32
# Must match BETTER_AUTH_SECRET in frontend/.env
BETTER_AUTH_SECRET=f2e6KynOcsOXWfk68/BHPrkPdChx2lY78VmBh2p+yCs=

# Better Auth URL for JWKS endpoint
# The backend fetches public keys from {BETTER_AUTH_URL}/api/auth/jwks
BETTER_AUTH_URL=http://localhost:3000

# JWT Configuration (fallback for symmetric verification)
JWT_ALGORITHM=HS256
JWT_EXPIRY_DAYS=7

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000
