# Generated from @specs/features/authentication/spec.md
# Frontend Environment Variables

# Backend API URL (FastAPI) - for task CRUD operations
NEXT_PUBLIC_API_URL=http://localhost:8000

# Database connection (Neon PostgreSQL) - shared with backend
# Used by Better Auth for user persistence (server-side only)
# Copy from backend/.env or your Neon dashboard
# IMPORTANT: Run 'npm run db:migrate-auth' after setting this to create tables
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Better Auth Configuration
# IMPORTANT: Must match BETTER_AUTH_SECRET in backend/.env
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-here-min-32-bytes

# Better Auth base URL (server-side, for callbacks)
BETTER_AUTH_URL=http://localhost:3000

# Better Auth client URL (client-side, for auth requests)
# This MUST point to the Next.js frontend, NOT the FastAPI backend
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# Setup instructions:
# 1. Copy this file to .env and fill in values
# 2. Ensure DATABASE_URL points to your Neon PostgreSQL database
# 3. Run: npm run db:migrate-auth (creates Better Auth tables)
# 4. Start the dev server: npm run dev
